test
common git commands mapping to sd commands
sd edit == git checkout
sd add == git add
sd opened == git status -s
sd sync == git pull
sd revert == git reset
sd submit == git commit + git push
dont use git checkout
make sure you call git push at the end to actually submit it to remote server!
